import React from 'react';
import './Footer.sass';

export const Footer = () => {
    return(
        <footer>
            <p className="footer__label">@JS + React Advanced 2018</p>
        </footer>
    );
};